package Life;
import CALab.Grid;

import java.util.*;

public class Society {

    public static Set<Integer> rebirth = new HashSet<Integer>();
    public static Set<Integer> death = new HashSet<Integer>();
    public static int percentAlive = 50;

    public Agent makeCell() {
        // randomly populate the grid with agents (use percentAlive)
        // manually initialize: clear to kill all agents, click on cells to change status
        return null;
    }
}
